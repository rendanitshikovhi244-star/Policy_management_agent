# Async PostgreSQL connection string.
# asyncpg uses the postgresql:// scheme internally; the +asyncpg suffix
# follows SQLAlchemy convention and is stripped at runtime.
DATABASE_URL=postgresql+asyncpg://insurance_user:insurance_pass@localhost:5432/insurance

# Gemini API key – required by google-adk
GOOGLE_API_KEY=your_google_api_key_here
